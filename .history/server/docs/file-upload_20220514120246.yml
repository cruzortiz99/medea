Create UploadCsv
---
tags:
  - Upload
parame:
  content:
    multipart/form-data:
      schema:
        properties:
          # The property name 'file' will be used for all files.
          csv:
            type: string
            format: binary