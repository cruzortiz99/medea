export enum PAGE_URL {
  HOME = "/",
  ALERTS_FAILURES = "/alerts-failures",
  ORDERS = "/orders",
  BUDGET = "/budget",
  EQUIPMENT_HISTORY = "/equipment-history",
  PREVENTIVE_MAINTENANCE = "/preventive-maintenance",
  UPDATE_DATA = "/update-data",
  DOCUMENTATION = "/docs",
  ABOUT = "/about",
  NOT_FOUND = "/not-found",
  TEST_PAGE = "/test-lab",
}
